# _README.md — HP-LEASE (Harbor Point lease-up initiative)
*(Kit exemplar — the ANNOTATING style: for live folders with well-named files. Routes and calls out
what matters; deliberately does not enumerate — the live directory listing outranks any file map.)*

*Part of a SmartFolder — protocol and conventions live in the root `CLAUDE.md`.*

**As of [260701]. Tier 2 — initiative narrative + status.** The one-line current status lives on the
status board (`00_HQ/00_STATUS_BOARD.md`); recency wins if the two disagree.

## What this initiative is

The lease-up of 12 Harbor Point to Meridian Logistics: LOI signed [260610], lease drafting underway,
target execution [260715]. Files here are dated `YYMMDD_<Topic>_<Qualifier>.ext` and read as a
timeline — browse the directory listing chronologically rather than expecting a map here.

## What matters most right now

- The **current draft** is the [260628] lease redline; everything earlier is superseded history.
- The **rent-escalation dispute** (CPI floor vs. fixed 3%) is the open negotiation point — see
  `260628_LeaseRedline_EscalationIssue_Summary.md` (sidecar carries the clause-by-clause positions).
- Dated decisions live in `DECISIONS.md`; document-by-document history in `INDEX.md`.

## Subfolders

- `Diligence/` — third-party reports on the building (own `_README.md`).
- `Superseded_Drafts/` — earlier LOI/lease versions, kept for history; don't cite as current.

---
*Derived from the files here as of [260701]; the raw files win on conflict. Decays when: a draft
newer than [260628] lands, the escalation point resolves, or files are added here. To refresh: ask
your AI assistant to update this guide.*
